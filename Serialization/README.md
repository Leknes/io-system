# Path Serialization System
Provides the `PathSerializer` class and the `PathDeserializer` class which can be used to serialize/deserialize objects at a path based on an `IDuplexDecorator<string>` object.
